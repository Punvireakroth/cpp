#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    int N = 0;
    cout << "input the array length";
    cin >> N;
    int arrN[N] = {1,2,4,3};
    for (int i; i < N; i++)
    {
        cout << arrN
    }
    return 0;
}